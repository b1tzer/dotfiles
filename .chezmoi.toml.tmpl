{{- $git_name  := promptStringOnce . "git_name"  "Git commit name (e.g. John Doe)" -}}
{{- $git_email := promptStringOnce . "git_email" "Git commit email (e.g. john@example.com)" -}}

[data]
    git_name  = {{ $git_name | quote }}
    git_email = {{ $git_email | quote }}

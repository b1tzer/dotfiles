# configs/
# Place machine-specific or shared non-sensitive configuration snippets here.
# Examples:
#   configs/git/gitconfig.template   - Git global config template
#   configs/ssh/config.template      - SSH client config template
#
# Files ending in .template will be processed by init_secrets.sh
# to substitute placeholders with values from secrets.local.env
